{
  "name": "Rawt",
  "version": "0.1.0",
  "description": "A different take on routers",
  "scripts": {
    "babel-node": "./node_modules/.bin/babel-node",
    "start": "DEBUG=*,-express:*,-babel*,-send ./node_modules/.bin/nodemon --exec npm run babel-node -- ./app/server",
    "test": "./node_modules/.bin/mocha --recursive -R spec --compilers js:babel/register",
    "testWatch": "./node_modules/.bin/mocha --recursive --watch -R spec --compilers js:babel/register",
    "compile":"babel index.js dist/rawter.js"
  },
  "author": "xananax@yelostudio.com",
  "license": "MIT",
  "dependencies": {
    "babel": "^4.7.16",
    "chai": "^3.0.0",
    "debug": "^2.2.0",
    "mocha": "^2.2.5",
    "nodemon": "^1.3.7",
    "path-to-regexp": "^1.2.0"
  },
  "engines": {
    "node": "0.12.0"
  }
}
